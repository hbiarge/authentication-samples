@page
@model SecureFakeModel
@{
    ViewData["Title"] = "Secure";
}
<h3>User claims</h3>

<dl>
    @foreach (var claim in User.Claims)
    {
        <dt>@claim.Type</dt>
        <dd>@claim.Value</dd>
    }
</dl>