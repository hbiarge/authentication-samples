<Page
    x:Class="UwpIdSrv.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UwpIdSrv"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Button HorizontalAlignment="Stretch" Content="Call API" Height="34" Margin="0,6,0,0" Click="CallApi" x:Name="CallApiButton">
        </Button>
        <ScrollViewer Grid.Row="1" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" VerticalScrollBarVisibility="Auto">
            <TextBlock  x:Name="TbResut" Margin="20,6,20,0" TextWrapping="WrapWholeWords"/>
        </ScrollViewer>
        <TextBlock Grid.Row="2" x:Name="TbStatus" Height="34" Margin="0,6,0,0"/>
    </Grid>
</Page>
